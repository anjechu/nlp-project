# 如何在PowerShell中输入游戏名（书名）？

## 📝 问题说明

您的文件结构是：
```
training/                    ← 训练脚本在这里
├── train_sentiment.py       ← 主训练脚本
└── steam_review/            ← JSON文件在这个子文件夹
    ├── steam_reviews_黑神话悟空.json
    ├── steam_reviews_只狼.json
    └── steam_reviews_艾尔登法环.json
```

**之前的代码问题**：代码只在 `training/` 目录查找文件，找不到 `steam_review/` 子文件夹中的文件。

**现在已修复**：代码会自动搜索 `steam_review/`、`steam_reviews/`、`data/` 等常见子目录。

---

## 🚀 在PowerShell中应该输入什么？

### 方法1️⃣：让程序自动找文件（最简单）✨

```powershell
# 进入training目录
cd D:\桌面\v1\nlp-project-copilot-add-s-dai-evaluation-model\training

# 使用通配符，程序会自动在steam_review子文件夹中查找
python train_sentiment.py --data_path "*.json" --epochs 10
```

**程序会输出**：
```
📂 正在扫描路径: *.json
💡 提示: 在子目录 'steam_review' 中找到文件
🔍 成功匹配到 3 个 JSON 文件，开始合并加载...
```

---

### 方法2️⃣：明确指定子文件夹路径（推荐）✅

```powershell
# 进入training目录
cd training

# 明确指定steam_review文件夹（注意：不是steam_reviewS，是steam_review）
python train_sentiment.py --data_path "steam_review/*.json" --epochs 10
```

**说明**：
- `steam_review/*.json` 表示加载 `steam_review/` 文件夹中所有 `.json` 文件
- **必须使用引号** `"steam_review/*.json"`（因为有通配符 `*`）
- 支持中文文件名

---

### 方法3️⃣：加载特定游戏的文件

```powershell
# 只训练"黑神话悟空"的评论
python train_sentiment.py --data_path "steam_review/steam_reviews_黑神话悟空.json" --epochs 10

# 只训练"只狼"的评论
python train_sentiment.py --data_path "steam_review/steam_reviews_只狼.json" --epochs 10

# 只训练"艾尔登法环"的评论
python train_sentiment.py --data_path "steam_review/steam_reviews_艾尔登法环.json" --epochs 10
```

**说明**：直接写文件名，程序会在子文件夹中查找。

---

### 方法4️⃣：先查看有哪些文件

```powershell
# 运行文件查找工具
python find_data_files.py
```

**会显示**：
```
📂 数据文件查找工具
══════════════════════════════════════════════

🔍 搜索目录: D:\...\training

✅ 找到 3 个JSON文件

📂 steam_review/ (3 个文件)
   1. steam_reviews_黑神话悟空.json
   2. steam_reviews_只狼.json
   3. steam_reviews_艾尔登法环.json

💡 使用建议:
   python train_sentiment.py --data_path 'steam_review/*.json' --epochs 10
```

**然后复制建议的命令运行即可！**

---

## 📋 完整命令示例

### 示例1：训练所有游戏的评论

```powershell
# 1. 打开PowerShell
# 2. 进入training目录
cd D:\桌面\v1\nlp-project-copilot-add-s-dai-evaluation-model\training

# 3. 运行训练（加载steam_review文件夹的所有JSON文件）
python train_sentiment.py --data_path "steam_review/*.json" --epochs 10 --batch_size 32
```

### 示例2：只训练"黑神话悟空"

```powershell
cd training
python train_sentiment.py --data_path "steam_review/steam_reviews_黑神话悟空.json" --epochs 10
```

### 示例3：训练多个参数

```powershell
python train_sentiment.py `
    --data_path "steam_review/*.json" `
    --epochs 10 `
    --batch_size 32 `
    --learning_rate 0.00002 `
    --auto_label
```

**说明**：
- 在PowerShell中，`` ` `` (反引号) 用于换行
- 可以把长命令分成多行，更容易阅读

---

## ⚠️ 常见错误

### ❌ 错误1：不使用引号

```powershell
# 错误
python train_sentiment.py --data_path steam_review/*.json

# 正确
python train_sentiment.py --data_path "steam_review/*.json"
```

**原因**：PowerShell会尝试展开 `*`，但必须用引号让Python处理。

---

### ❌ 错误2：文件夹名字错误

```powershell
# 如果你的文件夹叫 steam_review（单数）
--data_path "steam_review/*.json"  ✅ 正确

# 如果你的文件夹叫 steam_reviews（复数）
--data_path "steam_reviews/*.json"  ✅ 正确
```

**检查方法**：
```powershell
# 查看training目录下的文件夹
cd training
ls
```

---

### ❌ 错误3：路径中有空格

```powershell
# 如果路径包含空格，必须使用引号
--data_path "my data/steam_review/*.json"  ✅ 正确
```

---

## 🎯 快速参考表

| 你想做什么 | PowerShell命令 |
|-----------|---------------|
| 查看有哪些JSON文件 | `python find_data_files.py` |
| 训练所有游戏 | `python train_sentiment.py --data_path "steam_review/*.json" --epochs 10` |
| 只训练黑神话悟空 | `python train_sentiment.py --data_path "steam_review/steam_reviews_黑神话悟空.json" --epochs 10` |
| 只训练只狼 | `python train_sentiment.py --data_path "steam_review/steam_reviews_只狼.json" --epochs 10` |
| 只训练艾尔登法环 | `python train_sentiment.py --data_path "steam_review/steam_reviews_艾尔登法环.json" --epochs 10` |
| 查看所有参数 | `python train_sentiment.py --help` |

---

## ✨ 代码自动修复说明

### 修改了什么？

**修改前**（旧代码，有问题）：
```python
# 只在当前目录搜索
search_pattern = os.path.join(SCRIPT_DIR, "steam_reviews*.json")
DATA_FILES = glob.glob(search_pattern)

if not DATA_FILES:
    print("❌ 错误：在当前文件夹没有找到文件！")
    exit(1)
```

**问题**：
- 只在 `training/` 目录查找
- 找不到 `training/steam_review/` 子文件夹中的文件

---

**修改后**（新代码，已修复）：
```python
def find_data_files(pattern: str, search_subdirs: bool = True) -> List[str]:
    """智能搜索数据文件"""
    # 首先在当前目录搜索
    files = glob.glob(pattern)
    if files:
        return files
    
    # 如果没找到，搜索常见子目录
    common_subdirs = [
        'steam_reviews',
        'steam_review',      # 支持单数和复数
        'data',
        'training_data',
        ...
    ]
    
    for subdir in common_subdirs:
        if os.path.exists(subdir):
            files = glob.glob(os.path.join(subdir, pattern))
            if files:
                print(f"💡 提示: 在子目录 '{subdir}' 中找到文件")
                return files
    
    return []
```

**改进**：
1. ✅ 自动搜索子目录 `steam_review/`、`steam_reviews/`、`data/` 等
2. ✅ 提供友好的提示信息
3. ✅ 支持单数/复数文件夹名
4. ✅ 给出详细的错误提示和建议

---

## 📚 相关文档

- **QUICKSTART_CN.md** - 快速开始指南
- **POWERSHELL_USAGE_GUIDE.md** - PowerShell详细使用指南
- **DATA_PATH_FIX.md** - 技术细节和修复说明

---

## 💡 总结

**最简单的使用方式**：

```powershell
# 1. 进入training目录
cd training

# 2. 运行（会自动找到steam_review文件夹中的文件）
python train_sentiment.py --data_path "steam_review/*.json" --epochs 10
```

**或者让程序自动搜索**：

```powershell
python train_sentiment.py --data_path "*.json" --epochs 10
```

**不确定？先查看文件**：

```powershell
python find_data_files.py
```

---

**就是这么简单！** 🎉

如果还有问题，程序会给出详细的提示告诉你该怎么做。
